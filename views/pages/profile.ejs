<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
</head>

<body>
    <header>
        <%- include('../partials/header') %>
    </header>

    <main>
        <% userbooklist.forEach(function(book) { %>
        <a id="<%= book.id %>" href="/book">
            <img class="book-result" src="<%= book.image_url %>">
            <% }); %>
        </a>
    </main>

    <footer>
        <%- include('../partials/footer') %>
    </footer>

</body>
</html>
